<UserControl x:Class="WpfApp.Views.ControlView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:WpfApp.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <DockPanel>
            <Button Height="25" Width="100" Margin="0,0,0,0" >Create</Button>
            <Button Height="25" Width="100">Execute</Button>
            <Button Height="25" Width="100">Pause</Button>
            <Button Height="25" Width="100" Margin="0,0,0,0">Stop</Button>
            <Button Height="25" Width="100">Resume</Button>
        </DockPanel>
    </Grid>
</UserControl>
